This is all bash scripts on the camera system. Along with all saved images that are not on the website.
It handles both photo system logic, and rain wiping.

The photo queue is also stored within this document, but it is reset everytime "system_master.sh" is run
